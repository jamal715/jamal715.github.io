const year=document.getElementById('year'); if(year) year.textContent=new Date().getFullYear();
const menuBtn=document.getElementById('menuBtn'); const nav=document.querySelector('.nav'); menuBtn?.addEventListener('click',()=>nav.classList.toggle('open')); document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12}); document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const stars=[...document.querySelectorAll('#stars button')];
const avatarOptions=[...document.querySelectorAll('.avatar-option')]; avatarOptions.forEach(o=>o.addEventListener('click',()=>{avatarOptions.forEach(x=>x.classList.remove('selected'));o.classList.add('selected')})); const rating=document.getElementById('rating'); function paint(v){stars.forEach(s=>s.classList.toggle('active',Number(s.dataset.value)<=v)); if(rating) rating.value=v} paint(5); stars.forEach(s=>s.addEventListener('click',()=>paint(Number(s.dataset.value))));
function enc(s){return encodeURIComponent(s)}
document.getElementById('reviewForm')?.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('reviewName').value.trim();const org=document.getElementById('reviewOrg').value.trim();const text=document.getElementById('reviewText').value.trim();const r=document.getElementById('rating').value;const avatar=document.querySelector('input[name="avatar"]:checked')?.value||'ocean';const body=`QuantOra Review Submission\n\nName: ${name}\nCompany/Institution: ${org||'—'}\nRating: ${r}/5\nAvatar: ${avatar}\n\nReview:\n${text}\n\nPermission: I approve public display of my name, rating, selected avatar and review after moderation.`;window.location.href=`mailto:j.nasir25260@gmail.com?subject=${enc('QuantOra review submission — '+name)}&body=${enc(body)}`});
document.getElementById('requestForm')?.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('name').value.trim();const email=document.getElementById('email').value.trim();const need=document.getElementById('need').value;const problem=document.getElementById('problem').value.trim();const budget=document.getElementById('budget').value.trim();const timeline=document.getElementById('timeline').value.trim();const body=`QuantOra Project Request\n\nName: ${name}\nEmail: ${email}\nNeed: ${need}\nBudget: ${budget||'Not specified'}\nTimeline: ${timeline||'Not specified'}\n\nProblem / desired outcome:\n${problem}`;window.location.href=`mailto:j.nasir25260@gmail.com?subject=${enc('QuantOra project request — '+need)}&body=${enc(body)}`});
const canvas=document.getElementById('network'); const ctx=canvas?.getContext('2d'); let pts=[]; function resize(){if(!canvas)return;canvas.width=innerWidth*devicePixelRatio;canvas.height=820*devicePixelRatio;canvas.style.height='820px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);pts=Array.from({length:Math.min(70,Math.floor(innerWidth/18))},()=>({x:Math.random()*innerWidth,y:Math.random()*760,vx:(Math.random()-.5)*.18,vy:(Math.random()-.5)*.18}))}function draw(){if(!ctx)return;ctx.clearRect(0,0,innerWidth,820);for(const p of pts){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>innerWidth)p.vx*=-1;if(p.y<0||p.y>800)p.vy*=-1;ctx.beginPath();ctx.arc(p.x,p.y,1.4,0,Math.PI*2);ctx.fillStyle='rgba(0,77,115,.32)';ctx.fill()}for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){const a=pts[i],b=pts[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<125){ctx.strokeStyle=`rgba(0,77,115,${.09*(1-d/125)})`;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}requestAnimationFrame(draw)}window.addEventListener('resize',resize);resize();draw();